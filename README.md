# native_api_1c

Rust-обертка для Native API платформы 1C:Enterprise.

## Что это такое

**1C:Enterprise** - платформа для автоматизации бизнеса. Для расширения функциональности используется **Native API** - интерфейс для создания внешних компонентов.

**native_api_1c** - Rust-обертка для Native API, предоставляющая:

- Автоматическую генерацию кода через процедурные макросы
- Безопасную работу с памятью благодаря системе владения Rust
- Строгую типизацию для предотвращения ошибок времени выполнения
- Упрощенный декларативный синтаксис для описания компонентов

### Пример использования

**Традиционный подход (C++):**
```cpp
class MyComponent : public IComponentBase {
    // Реализация интерфейсов Native API
    // Управление памятью
    // Обработка ошибок
};
```

**С использованием native_api_1c (Rust):**
```rust
#[derive(AddIn)]
pub struct MyComponent {
    #[prop(ty = Int, name = "MyProp", readable, writable)]
    pub my_property: i32,
    
    #[func(name = "MyFunction")]
    #[param(ty = Int)]
    #[return_type(ty = Int)]
    pub my_function: fn(&Self, i32) -> i32,
}
```

### Применение

- Интеграция с внешними API
- Обработка данных
- Работа с файлами
- Криптографические операции
- Сетевые операции

### Быстрый старт

1. Создайте проект:
   ```bash
   cargo new my_1c_component --lib
   cd my_1c_component
   ```

2. Добавьте зависимость в `Cargo.toml`:
   ```toml
   [dependencies]
   native_api_1c = { git = "https://github.com/IgorKilipenko/native_api_1c_fork.git", branch = "main" }
   utf16_lit = "2.0"
   ```

3. Создайте компонент в `src/lib.rs`:
   ```rust
   use native_api_1c::{native_api_1c_macro::{AddIn, extern_functions}};
   
   #[derive(AddIn)]
   pub struct MyComponent {
       #[prop(ty = Int, name = "Counter", readable, writable)]
       pub counter: i32,
   }
   
   impl Default for MyComponent {
       fn default() -> Self { Self { counter: 0 } }
   }
   
   extern_functions! { MyComponent::default() }
   ```

## 📋 О проекте

Форк оригинального репозитория [Sebekerga/native_api_1c](https://github.com/Sebekerga/native_api_1c) с дополнительными улучшениями.

### 🎯 Основные цели

- Предоставление Rust-обертки для 1C Native API
- Упрощение разработки AddIn компонентов
- Эффективная работа с FFI интерфейсами
- Комплексное тестирование и обработка ошибок

## 🏗️ Архитектура

Библиотека разделена на три модуля:

### `native_api_1c_core`
- FFI обертки для взаимодействия с 1C Native API
- Типы данных для работы с параметрами 1C
- Интерфейсы для реализации AddIn компонентов
- Система ошибок с детализированными типами

### `native_api_1c_macro`
- `#[derive(AddIn)]` - автоматическая генерация реализации `AddInWrapper`
- Упрощенные атрибуты для декларативного описания компонентов
- Генерация кода для свойств, методов и функций

### `native_api_1c`
Объединяющий модуль с единым API.

## 🔧 Технические особенности

### Система типов
- `ParamValue` - enum для представления типов данных 1C
- `ParamValues` - коллекция параметров с индексированным доступом
- `AddInWrapper` - основной трейт для реализации AddIn компонентов

### Обработка ошибок
Использует `thiserror` для создания иерархии ошибок:
```rust
pub enum NativeApiError {
    Property(PropertyError),
    Method(MethodError),
    Memory(MemoryError),
    Initialization(InitializationError),
    Parameter(ParameterError),
    TypeConversion(TypeConversionError),
    Operation { message: String },
}
```

### FFI безопасность
- Безопасные обертки для всех FFI функций
- Проверка типов на этапе компиляции
- Управление памятью через RAII паттерны

## 🧪 Тестирование

### Покрытие тестами
- 73 теста в `native_api_1c_macro`
- 55 тестов в `native_api_1c_core`
- 100% покрытие основной функциональности

### Стратегия тестирования
- Mock-объекты для избежания небезопасных FFI вызовов
- Компиляционные тесты для проверки генерации кода
- Интеграционные тесты для проверки взаимодействия компонентов

## 📊 Статистика проекта

- Язык: Rust 100%
- Зависимости: `thiserror`, `darling`, `syn`, `quote`, `proc-macro2`
- Поддерживаемые платформы: Linux, Windows, macOS
- Версия Rust: 1.70+

## 🚀 Установка

### Рекомендуемый способ (через наш форк)

Добавьте в ваш `Cargo.toml`:

```toml
[dependencies]
native_api_1c = { git = "https://github.com/IgorKilipenko/native_api_1c_fork.git", branch = "main" }
utf16_lit = "2.0"
```

### Альтернативный способ (через crates.io)

Если вы хотите использовать оригинальную версию:

```toml
[dependencies]
native_api_1c = "0.10.5"
utf16_lit = "2.0"
```

### Установка конкретной версии

Для использования конкретной версии из нашего форка:

```toml
[dependencies]
# Текущая версия
native_api_1c = { git = "https://github.com/IgorKilipenko/native_api_1c_fork.git", tag = "v0.11.0-beta.1" }
utf16_lit = "2.0"
```

### Различия между версиями

| Версия | Источник | Особенности |
|--------|----------|-------------|
| **Наш форк** | GitHub | ✅ Улучшенная система ошибок<br>✅ Упрощенные атрибуты<br>✅ Комплексное тестирование<br>✅ Рефакторинг макросов |
| **Оригинал** | crates.io | ⚠️ Базовая функциональность<br>⚠️ Устаревшие атрибуты<br>⚠️ Ограниченное тестирование |

**Рекомендация**: Используйте наш форк для получения всех последних улучшений и исправлений.

### Переключение между версиями

Если вы уже используете оригинальную версию и хотите переключиться на наш форк:

1. **Обновите `Cargo.toml`**:
   ```toml
   [dependencies]
   # Замените эту строку:
   # native_api_1c = "0.10.5"
   # На эту:
   native_api_1c = { git = "https://github.com/IgorKilipenko/native_api_1c_fork.git", branch = "main" }
   ```

2. **Обновите зависимости**:
   ```bash
   cargo update
   ```

3. **Проверьте совместимость**:
   ```bash
   cargo check
   cargo test
   ```

**Примечание**: Наш форк полностью совместим с оригинальной версией, но предоставляет дополнительные возможности.

### Понимание тегов и версий

| Параметр | Описание | Пример |
|----------|----------|---------|
| `branch = "main"` | Последняя версия из ветки | Всегда актуально |
| `tag = "v0.10.6"` | Конкретная стабильная версия | Фиксированная версия |
| `tag = "v0.11.0-beta.1"` | Бета-версия с новыми возможностями | Экспериментальные функции |
| `rev = "abc123..."` | Конкретный коммит | Точная версия |

**Рекомендации:**
- **Для продакшена**: используйте `tag` с стабильной версией
- **Для разработки**: используйте `branch = "main"`
- **Для тестирования**: используйте бета-теги

## 📖 Документация

- [Примеры использования](USAGE_EXAMPLES.md) - подробные примеры и руководство по использованию
- [API документация](https://docs.rs/native_api_1c) - автогенерированная документация
- [Оригинальная документация 1C](nativeapi_srcdata/docs/native_api.txt) - официальная документация Native API

## 🔄 Совместимость

### Обратная совместимость
- Полная совместимость с существующим кодом
- Постепенная миграция на новые атрибуты
- Поддержка устаревших атрибутов `#[add_in_*]`

### Версионирование
Следует [Semantic Versioning](https://semver.org/):
- MAJOR - несовместимые изменения API
- MINOR - новая функциональность с обратной совместимостью
- PATCH - исправления ошибок

## 🛠️ Разработка

### Требования
- Rust 1.70+
- Cargo
- Git

### Сборка
```bash
git clone https://github.com/IgorKilipenko/native_api_1c_fork.git
cd native_api_1c_fork
cargo build
```

### Обновление зависимостей
```bash
# Обновление до последней версии из нашего форка
cargo update

# Обновление конкретной зависимости
cargo update -p native_api_1c

# Проверка доступных обновлений
cargo outdated
```

### Тестирование
```bash
cargo test
cargo test --test mod  # Запуск всех тестов
```

### Линтинг
```bash
cargo clippy
cargo fmt
```

## 📈 Планы развития

### Фаза 1: Стабилизация (Завершена ✅)
- [x] Улучшенная система ошибок
- [x] Упрощенные атрибуты
- [x] Комплексное тестирование

### Фаза 2: Оптимизация (В процессе 🟡)
- [x] Базовые трейты для коллекторов
- [ ] Унификация всех коллекторов
- [ ] Оптимизация генерации кода

### Фаза 3: Расширение (Планируется ⏳)
- [ ] Дополнительные типы данных
- [ ] Улучшенная документация
- [ ] Примеры интеграции

## 🤝 Участие в разработке

### Правила контрибуции
1. Создайте issue перед началом работы над новой функциональностью
2. Следуйте стилю кода проекта
3. Добавляйте тесты для новой функциональности
4. Обновляйте документацию при необходимости

### Процесс разработки
1. Fork репозитория
2. Создайте feature ветку
3. Внесите изменения
4. Добавьте тесты
5. Создайте Pull Request

## 📄 Лицензия

Проект распространяется под лицензией MIT. См. файл [LICENSE](LICENSE) для подробностей.

## 🙏 Благодарности

- Sebekerga - оригинальный автор библиотеки
- Сообщество Rust - за отличные инструменты и экосистему
- 1C - за предоставление Native API

## 📞 Поддержка

- Issues: [GitHub Issues](https://github.com/IgorKilipenko/native_api_1c_fork/issues)
- Discussions: [GitHub Discussions](https://github.com/IgorKilipenko/native_api_1c_fork/discussions)
